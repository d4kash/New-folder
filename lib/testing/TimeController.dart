import 'package:get/get.dart';

class TimeController {
  //var Time = (DateTime.now().hour.toString()+":"+DateTime.now().minute.toString()+":"+DateTime.now().second.toString()).obs;
  var count = 0.obs;

}